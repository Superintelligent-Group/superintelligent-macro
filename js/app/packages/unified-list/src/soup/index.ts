/**
 * Soup component - DEPRECATED
 *
 * This module has moved to @soup package.
 * These re-exports are for backwards compatibility.
 */

export {
  Soup,
  type SoupProps,
  useSoupQuery,
  type SoupQueryFilters,
  type SoupQueryResult,
  type EmailViewMode,
  SOUP_DEFAULTS,
  type SortMethod,
  type EmailView,
} from '@soup';
